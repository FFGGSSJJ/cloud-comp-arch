import dynam_scheduler
import resource_alert
import time, sys
import threading

import json







if __name__ == "__main__":

    memcachedAlt = resource_alert.ResourceAlert("memcached")



    # dScheduler = dynam_scheduler.DynamicScheduler(parsec_apps_tags)

    
    # for obj in containerJson[parsec_apps_tags[0]]:
    #     print(containerJson[parsec_apps_tags[0]][obj])

