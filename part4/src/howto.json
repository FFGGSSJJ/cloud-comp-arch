
{
    "parsec_blackscholes": {
        "cpu": "0-1",
        "memory": "",
        "command": "./run -a run -S parsec -p blackscholes -i native -n 8"
    },

    "parsec_canneal": {
        "cpu": "0-1",
        "memory": "",
        "command": "./run -a run -S parsec -p canneal -i native -n 8"
    },

    "parsec_dedup": {
        "cpu": "0-1",
        "memory": "",
        "command": "./run -a run -S parsec -p dedup -i native -n 8"
    },

    
    "parsec_ferret": {
        "cpu": "0-1",
        "memory": "",
        "command": "./run -a run -S parsec -p ferret -i native -n 8"
    },
    
    "parsec_freqmine": {
        "cpu": "0-1",
        "memory": "",
        "command": "./run -a run -S parsec -p freqmine -i native -n 4"
    },

    "splash2x_radix": {
        "cpu": "0-1",
        "memory": "",
        "command": "./run -a run -S splash2x -p radix -i native -n 8"
    },

    "parsec_vips": {
        "cpu": "0-1",
        "memory": "",
        "command": "./run -a run -S parsec -p vips -i native -n 4"
    }
}